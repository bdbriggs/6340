{
  "name": "dfc-contact-server",
  "version": "1.0.0",
  "private": true,
  "description": "Nodemailer contact form backend for Dumpster Fire Coding",
  "main": "server.js",
  "scripts": {
    "start": "node start-simple.js",
    "start:legacy": "node server.js",
    "dev": "nodemon start.js",
    "build": "echo 'No build step required for this Node.js app'",
    "docker:build": "docker build -t dfc-contact-server .",
    "docker:run": "docker run -p 3000:3000 --env-file .env dfc-contact-server",
    "docker:compose": "docker-compose up -d",
    "test": "echo 'No tests configured'",
    "deploy-test": "node deploy-test.js",
    "start:fallback": "node start-fallback.js",
    "start:simple": "node start-simple.js"
  },
  "author": "Baily Atchley",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.10",
    "express": "^4.18.2",
    "mysql2": "^3.15.1",
    "nodemailer": "^6.9.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
